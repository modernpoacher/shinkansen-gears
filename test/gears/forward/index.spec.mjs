import {
  expect
} from 'chai'

import Forward from '#gears/gears/forward'

describe('#gears/gears/forward', () => {
  describe('`Forward`', () => {
    it('is a function', () => {
      expect(Forward)
        .to.be.a('function')
    })
  })
})
